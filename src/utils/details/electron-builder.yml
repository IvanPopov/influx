productName: "details"
appId: "details"
artifactName: "${productName}.${ext}"

asar: true

extraMetadata:
  main: "dist/utils/details.js"

files:
  - "dist/utils/details.js"
  - "package.json"

# Windows configuration
win:
  target:
    - "zip"

directories:
  buildResources: "dist/utils"
  output: "release"

